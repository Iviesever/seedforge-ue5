# AI assistance and authorship

## Disclosure

SeedForge was implemented, tested, debugged, packaged, and documented by Codex using GPT-5.6 Sol under a user-approved project specification. The user explicitly chose not to participate in writing the code during the delivery window.

The user supplied the career goal, deadline, workspace boundary, agent-count limit, and approval of the scoped blueprint. Codex selected implementation details, wrote source/tests/scripts/documents, ran the toolchain, inspected logs and images, and recorded evidence.

## What must not be claimed

- The user must not say they independently hand-wrote this C++ implementation.
- Passing generated tests does not by itself prove the user's C++ knowledge.
- The repository must not be submitted where AI-generated work is prohibited.
- The project should not be described as production-ready, cross-platform, or a complete game.

## Responsible portfolio use

Appropriate description:

> I specified and supervised an AI-assisted UE 5.8 C++ engineering project, then studied, validated, and modified the resulting system. The repository discloses the assistance and includes reproducible tests and packaging evidence.

Before using SeedForge in a C++ interview, the user should be able to:

1. Reproduce the build/test/package commands.
2. Explain every component in `docs/CODE_WALKTHROUGH.md` without reading a script.
3. Re-derive the room placement, canonical sorting, hashing, and BFS invariants.
4. Explain cancellation versus interruption and the weak UObject boundary.
5. Make and test at least one personally authored change after the AI delivery.
