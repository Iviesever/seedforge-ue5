# Runtime HUD glyph coverage repair

The independent release audit requires three real, readable gameplay captures in Editor and package. Multiple original-resolution images reproduce missing left-hand Canvas glyph coverage. Receipt/PNG integrity alone cannot certify visual correctness.

Preserve existing gameplay and HUD information; repair only its rendering ownership. No Engine edits, binary assets, UI interaction, styling expansion, or changed acceptance thresholds. The original Goal explicitly delegates ordinary technical choices and forbids repeated clarification on them; this scoped implementation proceeds under that authorization rather than asking for a new product decision.
